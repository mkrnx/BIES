// Cowork venues are now server-backed (CoworkVenue) and added by members at
// runtime, so the curated seed list starts EMPTY. This file is kept only so the
// existing imports (CheckInModal, CoworkMap) keep resolving.
export const COWORK_SPOTS = [];

// Map defaults for the El Salvador overview — still used by CoworkMap.
export const EL_SALVADOR_CENTER = [13.72, -89.2];
export const EL_SALVADOR_ZOOM = 9;
