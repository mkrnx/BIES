services:
  bies-server:
    build:
      context: .
      target: server
    restart: unless-stopped
    env_file: server/.env
    environment:
      NODE_ENV: production
      PORT: "3001"
      DATABASE_URL: "file:../data/bies.db"
      NOSTR_PRIVATE_RELAY: "ws://bies-relay:7777"
      CORS_ORIGIN: "https://sovit.xyz/biestest"
    volumes:
      - bies-data:/app/data
      - bies-uploads:/app/uploads
      - relay-whitelist:/app/relay-whitelist
    networks:
      - bies-net
    deploy:
      resources:
        limits:
          memory: 512M
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  bies-relay:
    build:
      context: ./relay
    restart: unless-stopped
    volumes:
      - relay-data:/app/strfry-db
      - relay-whitelist:/app/data
    networks:
      - bies-net
    deploy:
      resources:
        limits:
          memory: 256M

  bies-auth-proxy:
    build:
      context: ./relay-proxy
    restart: unless-stopped
    environment:
      PROXY_PORT: "7778"
      UPSTREAM_RELAY: "ws://bies-relay:7777"
      WHITELIST_PATH: "/app/data/whitelist.txt"
      RELAY_URL: "wss://sovit.xyz/biestest/relay"
      AUTH_TIMEOUT_MS: "30000"
    volumes:
      - relay-whitelist:/app/data:ro
    networks:
      - bies-net
    depends_on:
      - bies-relay
    deploy:
      resources:
        limits:
          memory: 128M

  bies-nginx:
    build:
      context: .
      target: nginx
    restart: unless-stopped
    ports:
      - "127.0.0.1:8082:8080"
    depends_on:
      bies-server:
        condition: service_healthy
    networks:
      - bies-net
    deploy:
      resources:
        limits:
          memory: 128M

volumes:
  bies-data:
  bies-uploads:
  relay-data:
  relay-whitelist:

networks:
  bies-net:
    driver: bridge
