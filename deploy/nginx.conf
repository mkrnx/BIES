server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # ─── Allow file uploads up to 50 MB (matches Express multer limit) ─────
    client_max_body_size 50m;

    # ─── Security headers ────────────────────────────────────────────────────
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # ─── Gzip ────────────────────────────────────────────────────────────────
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;

    # ─── Static asset caching (Vite hashed filenames) ────────────────────────
    location /biestest/assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # ─── NIP-05 identity verification ─────────────────────────────────────────
    location /.well-known/nostr.json {
        proxy_pass http://bies-server:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        add_header Access-Control-Allow-Origin * always;
        add_header Cache-Control "max-age=300";
    }

    # ─── API proxy → backend server ──────────────────────────────────────────
    location /biestest/api/ {
        rewrite ^/biestest/api/(.*) /api/$1 break;
        proxy_pass http://bies-server:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ─── WebSocket proxy ─────────────────────────────────────────────────────
    location /biestest/ws {
        rewrite ^/biestest/ws(.*) /ws$1 break;
        proxy_pass http://bies-server:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 86400;
    }

    # ─── Nostr relay WebSocket proxy ──────────────────────────────────────────
    location /biestest/relay {
        proxy_pass http://bies-auth-proxy:7778;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 86400;
    }

    # ─── Upload files (if served locally) ────────────────────────────────────
    # The storage service returns URLs like /uploads/public/file.jpg, so we
    # need to serve them at /uploads/ (not just /biestest/uploads/).
    location /uploads/ {
        proxy_pass http://bies-server:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /biestest/uploads/ {
        rewrite ^/biestest/uploads/(.*) /uploads/$1 break;
        proxy_pass http://bies-server:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # ─── Bugs API proxy → bugs server ─────────────────────────────────────────
    location /biestest/bugs/api/ {
        rewrite ^/biestest/bugs/api/(.*) /$1 break;
        proxy_pass http://bies-bugs:4000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ─── Bugs frontend (static HTML) ───────────────────────────────────────────
    location = /biestest/bugs {
        default_type text/html;
        alias /usr/share/nginx/html/bugs/index.html;
    }

    location /biestest/bugs/ {
        alias /usr/share/nginx/html/bugs/;
        try_files $uri $uri/ /bugs/index.html;
    }

    # ─── SPA fallback for /biestest/ ─────────────────────────────────────────
    location /biestest/ {
        try_files $uri $uri/ /biestest/index.html;
    }

    # ─── Redirect root to /biestest/ ─────────────────────────────────────────
    location = / {
        return 301 /biestest/;
    }
}
